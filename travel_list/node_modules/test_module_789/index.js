function add(a, b) {
    return a + b;
}

// Example function that multiplies two numbers
function multiply(a, b) {
    return a * b;
}

// Export the functions so they can be used by consumers of your package
module.exports = {
    add,
    multiply
};